#!/usr/bin/env node
/**
 * hospitality-mcp — entry point.
 *
 * The MCP server wiring (stdio transport + tool registration) is added in a
 * later build phase. For now this is an intentional placeholder so the project
 * compiles cleanly from the very first commit.
 */

function main(): void {
  // Phase 4 wires up the MCP server here.
  console.error("hospitality-mcp: not yet implemented. See README.md.");
}

main();
